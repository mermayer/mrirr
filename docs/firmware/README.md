# Web installer firmware

The approved `mrirr-factory-current.bin` file is placed in this directory only
for GitHub Pages deployment. Until that release audit is complete, the installer
reads `available: false` from `../release.json` and keeps its install button
disabled.
